// src/config.ts

export const DEFAULT_DELAY_MS = 50;
export const MARGIN = 20; // Margin around the drawing
export const STORAGE_KEY = 'lineDrawerSettings'; // Key for localStorage

export enum AngleMode {
  Fraction = 'fraction',
  List = 'list'
} 